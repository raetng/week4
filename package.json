{
  "name": "weather-reports-app",
  "version": "1.0.0",
  "description": "A simple weather reports web application with SQLite storage",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest --coverage --coverageThreshold='{\"global\":{\"branches\":60,\"functions\":60,\"lines\":70,\"statements\":70}}'",
    "test:ci": "jest --coverage --coverageReporters=text --coverageReporters=lcov --coverageReporters=json",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "keywords": [
    "weather",
    "express",
    "sqlite",
    "nodejs"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "sql.js": "^1.10.3",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageDirectory": "coverage",
    "collectCoverageFrom": [
      "app.js"
    ],
    "testMatch": [
      "**/*.test.js"
    ]
  }
}
